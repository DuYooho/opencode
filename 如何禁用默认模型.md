# 如何不使用 OpenCode 官方免费模型，完全使用自定义模型

## 问题
您想避免使用 `opencode/big-pickle` 和 `opencode/gpt-5-nano` 等官方免费模型，只使用在 `opencode.jsonc` 中定义的自定义模型。

## 解决方案

### 方法 1: 使用 `disabled_providers` 禁用 opencode 提供商（推荐）

在您的 `opencode.json` 或 `opencode.jsonc` 配置文件中添加：

```json
{
  "$schema": "https://opencode.ai/config.json",
  "disabled_providers": ["opencode"],
  "provider": {
    "anthropic": {
      "options": {
        "apiKey": "${ANTHROPIC_API_KEY}"
      }
    },
    "openai": {
      "options": {
        "apiKey": "${OPENAI_API_KEY}"
      }
    }
  }
}
```

这样配置后，OpenCode 将完全忽略 `opencode` 提供商，包括其所有免费模型。

### 方法 2: 使用 `enabled_providers` 白名单模式（更严格）

如果您想更严格地控制，只允许特定的提供商：

```json
{
  "$schema": "https://opencode.ai/config.json",
  "enabled_providers": ["anthropic", "openai", "google"],
  "provider": {
    "anthropic": {
      "options": {
        "apiKey": "${ANTHROPIC_API_KEY}"
      }
    },
    "openai": {
      "options": {
        "apiKey": "${OPENAI_API_KEY}"
      }
    },
    "google": {
      "options": {
        "apiKey": "${GOOGLE_API_KEY}"
      }
    }
  }
}
```

这种方式下，**只有**列表中的提供商会被启用，其他所有提供商（包括 opencode）都会被忽略。

## 配置文件位置

您可以将配置文件放在以下位置之一：

1. **全局配置**（所有项目通用）:
   ```
   ~/.config/opencode/opencode.json
   ```

2. **项目配置**（仅当前项目）:
   ```
   /path/to/your/project/opencode.json
   或
   /path/to/your/project/.opencode/opencode.json
   ```

项目配置的优先级高于全局配置。

## 验证配置是否生效

运行以下命令检查可用的模型：

```bash
opencode models
```

如果配置成功，您应该**不会**看到 `opencode/big-pickle` 或 `opencode/gpt-5-nano`，只会看到您配置的提供商的模型。

也可以检查特定提供商：

```bash
opencode models opencode
# 应该显示 "No models found" 或类似信息
```

## 完整示例

### 示例 1: 只使用 Anthropic Claude

```json
{
  "$schema": "https://opencode.ai/config.json",
  "disabled_providers": ["opencode"],
  "model": "anthropic/claude-sonnet-4-5",
  "provider": {
    "anthropic": {
      "options": {
        "apiKey": "${ANTHROPIC_API_KEY}"
      },
      "models": {
        "claude-sonnet-4-5": {
          "name": "Claude Sonnet 4.5"
        },
        "claude-haiku-4-5": {
          "name": "Claude Haiku 4.5"
        }
      }
    }
  }
}
```

### 示例 2: 使用本地模型

```json
{
  "$schema": "https://opencode.ai/config.json",
  "disabled_providers": ["opencode"],
  "model": "local-llm/qwen-coder-32b",
  "provider": {
    "local-llm": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "本地 LLM",
      "options": {
        "baseURL": "http://localhost:8000/v1",
        "apiKey": "not-needed"
      },
      "models": {
        "qwen-coder-32b": {
          "name": "Qwen 2.5 Coder 32B"
        }
      }
    }
  }
}
```

## 常见问题

### Q: 配置后还是看到 opencode 提供商？

**A:** 请检查：
1. JSON 语法是否正确（使用 `jq . opencode.json` 验证）
2. 配置文件是否在正确的位置
3. 如果使用 `opencode serve` 或 `opencode web`，需要重启服务

### Q: 禁用后完全没有模型可用？

**A:** 确保：
1. 至少配置了一个提供商
2. 提供商有有效的 API key
3. API key 设置为环境变量或直接写在配置中

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
export OPENAI_API_KEY="sk-..."
```

### Q: disabled_providers 和 enabled_providers 有什么区别？

**A:**
- `disabled_providers`: 黑名单模式，禁用特定提供商，其他提供商正常加载
- `enabled_providers`: 白名单模式，**只**启用列表中的提供商，其他全部禁用

## 技术细节

配置选项定义在：
- `/packages/opencode/src/config/config.ts:920-924`

实现逻辑在：
- `/packages/opencode/src/provider/provider.ts:676-689`

```typescript
const disabled = new Set(config.disabled_providers ?? [])
const enabled = config.enabled_providers ? new Set(config.enabled_providers) : null

function isProviderAllowed(providerID: string): boolean {
  if (enabled && !enabled.has(providerID)) return false
  if (disabled.has(providerID)) return false
  return true
}
```

## 更多资源

- 完整文档：[TRACES_AND_CUSTOM_MODELS_DOCUMENTATION.md](./TRACES_AND_CUSTOM_MODELS_DOCUMENTATION.md)
- 配置示例：[examples/](./examples/)
- OpenCode 文档：https://opencode.ai/docs/providers

## 总结

要完全避免使用 OpenCode 官方免费模型：

1. ✅ 在 `opencode.json` 中添加 `"disabled_providers": ["opencode"]`
2. ✅ 配置您想使用的提供商（如 anthropic、openai 等）
3. ✅ 设置相应的 API keys
4. ✅ 运行 `opencode models` 验证配置

这样就可以确保只使用您在配置文件中明确定义的模型了！
