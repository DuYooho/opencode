# Qwen3 和 Qwen3-coder 工具渲染差异

## 快速回答

**问题**：Qwen3 和 Qwen3-coder 在渲染工具方面有什么不同？

**答案**：
- **Qwen3** 使用 **JSON 格式** 进行工具调用
- **Qwen3-coder** 使用 **XML 格式** 进行工具调用

这个差异由 **Vercel AI SDK** 自动处理，对用户完全透明。

---

## 核心发现

### 1. 工具格式差异

#### Qwen3（通用模型）- JSON 格式

```json
{
  "type": "function",
  "function": {
    "name": "read_file",
    "arguments": "{\"path\": \"/home/user/file.txt\"}"
  }
}
```

#### Qwen3-coder（代码模型）- XML 格式

```xml
<function_call>
  <name>read_file</name>
  <parameters>
    <path>/home/user/file.txt</path>
  </parameters>
</function_call>
```

### 2. 为什么存在这种差异？

1. **模型训练差异**
   - Qwen3：通用对话和任务，使用 JSON 格式训练
   - Qwen3-coder：专注代码任务，使用 XML 格式训练（可能针对代码工具调用优化）

2. **API 实现不同**
   - 阿里云 DashScope API 对不同模型系列使用不同的工具格式
   - Qwen3 系列：标准 OpenAI 兼容格式（JSON）
   - Qwen3-coder 系列：自定义格式（XML）

3. **SDK 自动识别**
   - Vercel AI SDK 根据模型 ID 和提供者自动选择正确格式
   - OpenCode 不需要额外配置

### 3. 对用户的影响

✅ **好消息**：对大多数用户**完全透明**

OpenCode 通过 AI SDK 自动处理两种格式，您无需关心底层实现。

⚠️ **需要注意的场景**：

1. **直接调用 API**：如果不使用 SDK 直接调用阿里云 API
2. **自定义工具开发**：如果在 OpenCode 之外实现自定义工具
3. **调试和日志**：查看原始 API 请求时会看到不同格式

---

## 如何验证工具格式

### 方法 1：导出会话数据

```bash
# 导出增强会话数据
opencode export --enhanced <session-id> > session.json

# 查看工具调用
cat session.json | jq '.callChain[] | select(.toolCalls) | .toolCalls'
```

### 方法 2：启用调试日志

```bash
# 设置调试环境变量
export DEBUG=ai:*

# 运行 OpenCode
opencode run "使用 read 工具读取文件"
```

### 方法 3：代理抓包

```bash
# 使用 HTTP 代理查看实际请求
export HTTP_PROXY=http://localhost:8888
opencode run "测试工具调用"
```

---

## 模型到格式的映射

| 模型系列 | 工具格式 | SDK 包 | 说明 |
|---------|---------|--------|------|
| **Qwen3** | JSON | `@ai-sdk/openai-compatible` | 通用模型，标准 JSON 格式 |
| **Qwen3-coder** | XML | `@ai-sdk/openai-compatible` | 代码模型，XML 格式 |
| **Qwen3-VL** | JSON | `@ai-sdk/openai-compatible` | 视觉模型，JSON 格式 |
| **GPT-4/5** | JSON | `@ai-sdk/openai` | OpenAI 标准 JSON 格式 |
| **Claude** | XML-like | `@ai-sdk/anthropic` | Anthropic 自定义格式 |
| **Gemini** | JSON | `@ai-sdk/google` | Google 自定义 JSON 格式 |

---

## 配置和自定义

### 当前状态：无法直接配置

OpenCode 中**没有配置选项**强制使用特定工具格式。格式由以下因素自动确定：

1. **模型 ID**：`qwen3` vs `qwen3-coder`
2. **提供者包**：`@ai-sdk/openai-compatible`
3. **AI SDK 逻辑**：自动检测并使用正确格式

### 如果需要特定格式

#### 选项 1：选择合适的模型

```jsonc
// opencode.jsonc
{
  "model": "qwen/qwen3",        // 使用 JSON 格式
  // 或
  "model": "qwen/qwen3-coder"   // 使用 XML 格式
}
```

#### 选项 2：创建自定义提供者（高级）

```typescript
// .opencode/provider/custom-qwen.ts
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const customQwen = createOpenAICompatible({
  baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
  // 添加自定义工具格式转换逻辑
});
```

#### 选项 3：提交功能请求

如果您需要明确控制工具格式：
- 在 OpenCode 仓库提 Issue
- 在 Vercel AI SDK 仓库提 Feature Request

---

## 相关代码位置

### OpenCode 代码库

```
/packages/opencode/src/
├── session/
│   ├── prompt.ts                 # 工具定义（JSON Schema）
│   └── llm.ts                    # LLM 调用编排
├── provider/
│   ├── provider.ts               # 提供者加载
│   ├── transform.ts              # 参数转换
│   └── sdk/openai-compatible/    # 自定义 SDK 实现
```

### 关键函数

1. **工具定义** (`session/prompt.ts`):
   ```typescript
   const aiTool = tool({
     id: item.id,
     description: item.description,
     inputSchema: jsonSchema(schema),  // 定义为 JSON Schema
     execute: async (params) => { ... }
   })
   ```

2. **格式转换** (由 AI SDK 处理):
   - JSON → 发送给 Qwen3 API
   - XML → 发送给 Qwen3-coder API

---

## 常见问题

### Q1: 为什么不能配置工具格式？

**A:** 工具格式由 AI SDK 和模型提供者的 API 实现决定。强制使用错误的格式会导致模型无法理解工具调用。

### Q2: 这会影响工具的功能吗？

**A:** 不会。两种格式都传达相同的信息（工具名称和参数），只是表示方式不同。OpenCode 确保两种格式都能正常工作。

### Q3: 如何知道我的模型使用哪种格式？

**A:** 
- 通用规则：Qwen3 系列用 JSON，Qwen3-coder 系列用 XML
- 验证方法：使用上面的三种验证方法查看实际格式

### Q4: 能混用两种格式吗？

**A:** 不能。每个模型只支持一种格式。尝试使用错误的格式会导致工具调用失败。

### Q5: 自定义工具需要适配不同格式吗？

**A:** 不需要。在 OpenCode 中定义自定义工具时：
- 使用标准的 Zod/JSON Schema 定义参数
- AI SDK 自动转换为模型需要的格式

---

## 最佳实践

### 1. 选择合适的模型

- **代码任务**：使用 `qwen3-coder`（虽然是 XML，但针对代码优化）
- **通用任务**：使用 `qwen3`（JSON 格式，通用性更好）
- **不确定**：两个都试试，选性能更好的

### 2. 调试时的建议

```bash
# 1. 先导出会话
opencode export --enhanced <session-id> > debug.json

# 2. 查看工具调用
jq '.callChain[] | select(.toolCalls) | {
  model: .model,
  tools: .toolCalls
}' debug.json

# 3. 检查是否有工具调用错误
jq '.callChain[] | select(.toolCalls) | .toolCalls[] | select(.status != "success")' debug.json
```

### 3. 性能考虑

- **XML 格式**：文本长度可能稍长，但结构清晰
- **JSON 格式**：更紧凑，解析性能可能稍好
- **实际差异**：对用户影响几乎可以忽略

---

## 延伸阅读

### 完整文档

- [TOOL_RENDERING_FORMATS.md](./TOOL_RENDERING_FORMATS.md) - 完整的工具渲染格式文档（中英双语）
- [TOOLS_DOCUMENTATION.md](./TOOLS_DOCUMENTATION.md) - 所有工具的完整文档
- [MODEL_PROMPT_RENDERING.md](./MODEL_PROMPT_RENDERING.md) - 模型提示词渲染差异

### 外部资源

- [Vercel AI SDK 文档](https://sdk.vercel.ai/docs)
- [阿里云 DashScope 文档](https://help.aliyun.com/zh/dashscope/)
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling)

---

## 总结

| 方面 | Qwen3 | Qwen3-coder |
|-----|-------|-------------|
| **工具格式** | JSON | XML |
| **用例** | 通用任务 | 代码任务 |
| **SDK 包** | `@ai-sdk/openai-compatible` | `@ai-sdk/openai-compatible` |
| **用户影响** | 透明（自动处理） | 透明（自动处理） |
| **可配置性** | 否（由模型决定） | 否（由模型决定） |
| **性能差异** | 几乎无 | 几乎无 |

**关键点**：OpenCode 通过 Vercel AI SDK 自动处理两种格式，用户无需关心底层实现细节。
