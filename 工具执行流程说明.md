# 工具执行流程说明

## 问题
看一下每个tool输出的指令具体是如何被执行的。

## 简短回答

每个工具的输出指令执行流程如下：

```
用户请求 
  ↓
AI 模型决定调用工具
  ↓
生成工具调用 (tool-call 事件)
  ↓
SessionProcessor 接收并执行
  ↓
工具的 execute() 函数运行
  ↓
返回结果 (tool-result 事件)
  ↓
结果发送回 AI 模型
  ↓
AI 处理结果并继续对话
```

## 详细流程

### 1. 工具定义阶段

工具通过 `Tool.define()` 定义，包含：
- **ID**: 工具名称 (如 "bash", "read", "edit")
- **Description**: 给 AI 看的自然语言描述
- **Parameters**: Zod 模式定义的参数
- **Execute 函数**: 实际执行逻辑

**示例** (bash 工具):
```typescript
export const BashTool = Tool.define("bash", async () => {
  return {
    description: "执行 shell 命令...",
    parameters: z.object({
      command: z.string(),
      timeout: z.number().optional(),
      workdir: z.string().optional()
    }),
    async execute(params, ctx) {
      // 1. 验证工作目录
      // 2. 解析命令
      // 3. 检查权限
      // 4. 执行命令
      // 5. 返回结果
      return {
        title: "命令已执行",
        output: "stdout + stderr",
        metadata: { exitCode: 0 }
      }
    }
  }
})
```

### 2. 工具注册阶段

所有工具在 `registry.ts` 中注册：
- **20+ 内置工具**: bash, read, edit, write, glob, grep, task 等
- **自定义工具**: 从 `.opencode/tool/*.{js,ts}` 加载
- **工具过滤**: 根据模型类型过滤可用工具

### 3. AI 模型调用

当 AI 模型被调用时：
```typescript
// 在 llm.ts 中
return streamText({
  model: language,
  messages: [...],
  tools,  // <-- 所有可用工具传递给模型
  // ...
})
```

工具会被转换成不同格式：
- **OpenAI**: JSON 模式
- **Anthropic**: XML 模式
- **其他**: OpenAI 兼容格式

### 4. AI 决策

AI 模型接收到：
- 系统提示词（如何使用工具）
- 工具定义（每个工具做什么）
- 对话历史
- 用户请求

然后决定：
- 调用哪个工具
- 使用什么参数
- 是否并行调用多个工具

**示例决策**:
```
用户: "显示 src/ 中的文件"
  ↓
AI 思考: "需要列出目录 → 使用 bash 工具"
  ↓
AI 生成:
{
  "toolName": "bash",
  "input": {
    "command": "ls -la src/",
    "description": "列出 src 目录中的文件"
  }
}
```

### 5. 工具执行 (SessionProcessor)

`SessionProcessor` 处理工具执行生命周期：

```typescript
switch (value.type) {
  case "tool-input-start":
    // AI 开始生成工具调用
    // → 创建状态为 "pending" 的工具部分
    break
    
  case "tool-call":
    // AI 完成生成工具调用
    // → 验证参数（Zod 模式检查）
    // → 更新状态为 "running"
    // → 调用工具的 execute() 函数
    break
    
  case "tool-result":
    // 工具执行成功
    // → 更新状态为 "completed"
    // → 存储输出
    // → 发送结果回 AI 模型
    break
    
  case "tool-error":
    // 工具执行失败
    // → 更新状态为 "error"
    // → 存储错误消息
    // → AI 可以重试或请求澄清
    break
}
```

### 6. 工具执行细节

以 bash 工具为例，执行时：

```typescript
async execute(params, ctx) {
  // 步骤 1: 验证工作目录
  const cwd = params.workdir || Instance.directory
  
  // 步骤 2: 解析命令 (使用 tree-sitter)
  const tree = await parser().then((p) => p.parse(params.command))
  
  // 步骤 3: 检查权限
  await ctx.ask({
    permission: "bash",
    patterns: ["ls"],  // 要检查的命令模式
    metadata: { command: params.command }
  })
  // 如果用户拒绝 → 抛出 PermissionNext.RejectedError
  
  // 步骤 4: 执行命令
  const result = await Shell.exec({
    command: params.command,
    cwd,
    timeout,
  })
  
  // 步骤 5: 返回结果
  return {
    title: params.description,
    output: result.stdout + result.stderr,
    metadata: {
      exitCode: result.exitCode,
      stdout: result.stdout,
      stderr: result.stderr
    }
  }
}
```

### 7. 结果处理

工具返回结果后：

```typescript
// 1. 结果存储在数据库
await Session.updatePart({
  ...toolPart,
  state: {
    status: "completed",
    output: result.output,
    metadata: result.metadata,
    time: { start, end }
  }
})

// 2. 结果发送回 AI 模型
streamInput.messages.push({
  role: "tool",
  content: [{
    type: "tool-result",
    toolCallId: "call_123",
    toolName: "bash",
    result: result.output
  }]
})

// 3. AI 处理结果
// - 可以调用更多工具
// - 可以问后续问题
// - 可以提供最终答案

// 4. 循环继续，直到 AI 提供文本响应
```

## 完整示例

**用户请求**: "创建一个文件 test.txt，内容为 'Hello World'，然后读取它"

### 执行流程：

**步骤 1**: AI 调用 write 工具
```json
{
  "toolName": "write",
  "input": {
    "filePath": "test.txt",
    "content": "Hello World"
  }
}
```
→ WriteTool.execute() 运行
→ 检查权限
→ 写入文件
→ 返回: "文件创建成功"

**步骤 2**: AI 调用 read 工具
```json
{
  "toolName": "read",
  "input": {
    "filePath": "test.txt"
  }
}
```
→ ReadTool.execute() 运行
→ 检查权限
→ 读取文件
→ 返回: "Hello World"

**步骤 3**: AI 提供最终响应
```
"我已创建 test.txt，内容为 'Hello World'，并验证了内容匹配。"
```

## 工具状态

工具执行有以下状态：

1. **pending**: AI 正在生成工具调用
2. **running**: 工具正在执行
3. **completed**: 执行成功
4. **error**: 执行失败

## 权限系统

许多工具在执行前需要用户权限：

```typescript
await ctx.ask({
  permission: "bash",           // 权限类型
  patterns: ["rm -rf *"],      // 要检查的模式
  always: ["rm", "sudo"],      // 总是询问这些
  metadata: { command: "..." } // 附加上下文
})
```

**权限类型**:
- **bash**: 命令执行
- **read**: 文件读取
- **edit**: 文件修改
- **write**: 文件创建
- **external_directory**: 访问项目外部
- **doom_loop**: 重复的工具调用

**权限状态**:
1. **总是允许**: 模式匹配代理的允许列表 → 自动批准
2. **总是拒绝**: 模式匹配拒绝列表 → 自动拒绝
3. **询问用户**: 提示用户做决定 → 显示对话框
4. **被拒绝**: 用户拒绝 → 抛出错误，停止执行

## 错误处理

### 参数验证错误

```typescript
// 自动使用 Zod 验证参数
try {
  toolInfo.parameters.parse(args)
} catch (error) {
  throw new Error(
    `${toolId} 工具使用了无效参数: ${error}。
     请重写输入以满足预期的模式。`
  )
}
// AI 看到错误 → 重新生成正确的参数
```

### 执行错误

```typescript
try {
  const result = await someOperation()
  return { title, output: result, metadata: {} }
} catch (error) {
  // 错误被捕获并作为 tool-error 事件发送
  // AI 看到错误 → 可以重试或请求澄清
  throw new Error(`执行失败: ${error.message}`)
}
```

### AI SDK 自动修复

AI SDK 可以自动修复某些错误：
- **大小写错误**: "Read" → "read"
- **无效工具**: 路由到 "invalid" 工具进行错误处理

## 高级功能

### 1. 输出截断

大型输出会自动截断：
- **限制**: 10,000 行 或 100 KB
- **超出时**: 完整输出保存到 `.opencode/output/{hash}.txt`
- **返回**: 截断的内容 + 指向完整文件的路径

### 2. 并行工具调用

某些 AI 模型支持并行执行：
```json
{
  "toolCalls": [
    { "toolName": "read", "input": { "filePath": "file1.txt" } },
    { "toolName": "read", "input": { "filePath": "file2.txt" } },
    { "toolName": "bash", "input": { "command": "git status" } }
  ]
}
```
→ 三个工具同时执行
→ 结果收集后一起发送回 AI

### 3. 文件附件

工具可以返回文件附件（图片、PDF）：
```typescript
return {
  title: "图片读取成功",
  output: "图片: example.png",
  attachments: [{
    type: "file",
    file: {
      name: "example.png",
      type: "image/png",
      data: base64EncodedData
    }
  }]
}
```
→ AI 接收图片并可以分析（如果支持视觉）

## 代码位置

| 组件 | 文件路径 |
|------|---------|
| 工具接口 | `/packages/opencode/src/tool/tool.ts` |
| 工具注册表 | `/packages/opencode/src/tool/registry.ts` |
| Bash 工具 | `/packages/opencode/src/tool/bash.ts` |
| Read 工具 | `/packages/opencode/src/tool/read.ts` |
| Edit 工具 | `/packages/opencode/src/tool/edit.ts` |
| LLM 调用 | `/packages/opencode/src/session/llm.ts` |
| 会话处理器 | `/packages/opencode/src/session/processor.ts` |
| 权限系统 | `/packages/opencode/src/permission/next.ts` |

## 调试方法

### 1. 启用调试日志

```bash
export OPENCODE_LOG_LEVEL=debug
```

### 2. 查看工具调用

在 UI 中，每个工具调用显示：
- 工具名称
- 输入参数
- 执行状态
- 输出结果
- 执行时间
- 元数据

### 3. 导出会话分析

```bash
opencode export <session-id> > session.json

# 查看所有工具调用
jq '.messages[] | select(.role == "assistant") | .parts[] | select(.type == "tool")' session.json
```

## 完整文档

详细文档请参阅: [TOOL_EXECUTION_FLOW.md](./TOOL_EXECUTION_FLOW.md)

这个文档包含：
- ✅ 完整的 10 部分详细说明
- ✅ 真实代码示例
- ✅ 完整的执行追踪
- ✅ 高级功能说明
- ✅ 调试指南
- ✅ 中英双语

## 总结

OpenCode 的工具执行是一个完整的循环：

1. **工具定义**: 使用 `Tool.define()` 创建工具
2. **工具注册**: 通过 `ToolRegistry` 加载
3. **AI 调用**: 模型通过 Vercel AI SDK 调用工具
4. **流式传输**: 工具调用通过事件流传输
5. **执行**: `SessionProcessor` 处理执行生命周期
6. **结果处理**: 输出返回给 AI
7. **迭代**: 循环继续直到任务完成

这个系统提供了：
- ✅ 类型安全（TypeScript + Zod）
- ✅ 权限控制（用户批准）
- ✅ 错误处理（验证 + 重试）
- ✅ 性能优化（并行执行 + 截断）
- ✅ 可扩展性（自定义工具 + 插件）
