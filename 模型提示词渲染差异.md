# 不同模型渲染系统提示词的方式

## 快速回答

你发现的 Qwen3-coder 和 Qwen3 系列渲染后的系统提示词不一样，这个观察很有意思。但根据代码分析，**OpenCode 实际上对 Qwen3-coder 和 Qwen3 使用完全相同的系统提示词和参数**。

## 核心发现

### 1. 提示词选择逻辑

在 `/packages/opencode/src/session/system.ts` 中：

```typescript
export function provider(model: Provider.Model) {
  if (model.api.id.includes("gpt-5")) return [PROMPT_CODEX]
  if (model.api.id.includes("gpt-") || model.api.id.includes("o1") || model.api.id.includes("o3"))
    return [PROMPT_BEAST]
  if (model.api.id.includes("gemini-")) return [PROMPT_GEMINI]
  if (model.api.id.includes("claude")) return [PROMPT_ANTHROPIC]
  return [PROMPT_ANTHROPIC_WITHOUT_TODO]  // ← qwen.txt，Qwen 和所有其他模型
}
```

**关键点**:
- Qwen3-coder 和 Qwen3 都不匹配任何特定模式
- 因此都使用默认的 `PROMPT_ANTHROPIC_WITHOUT_TODO`
- 这个常量实际上是从 `qwen.txt` 导入的

### 2. 模型参数也相同

在 `/packages/opencode/src/provider/transform.ts` 中：

```typescript
export function temperature(model: Provider.Model) {
  const id = model.id.toLowerCase()
  if (id.includes("qwen")) return 0.55  // 所有 qwen 模型
  // ...
}

export function topP(model: Provider.Model) {
  const id = model.id.toLowerCase()
  if (id.includes("qwen")) return 1  // 所有 qwen 模型
  // ...
}
```

**参数设置**:
- Temperature: `0.55` （两者相同）
- TopP: `1` （两者相同）

## 不同模型的提示词映射

| 模型类型 | 提示词文件 | 特点 |
|---------|----------|-----|
| GPT-5 | `codex_header.txt` | 包含前端设计、Git 指令 |
| GPT-3.5/4, O1, O3 | `beast.txt` | 类似 anthropic.txt |
| Gemini | `gemini.txt` | Gemini 优化 |
| Claude | `anthropic.txt` | **包含 TodoWrite 工具指令** |
| Qwen (所有) | `qwen.txt` | **简洁版本，无 TodoWrite** |
| 其他模型 | `qwen.txt` | 默认通用提示词 |

## 主要提示词差异

### anthropic.txt (Claude)
```
- 包含详细的 TodoWrite 工具使用说明
- 强调任务管理和规划
- "You are OpenCode, the best coding agent on the planet."
- 包含专业客观性指南
```

### qwen.txt (Qwen 和默认)
```
- 不包含 TodoWrite 工具
- "You are opencode, an interactive CLI tool"
- 更简洁直接
- 强调少于 4 行的简短回复
- 明确说明：不要添加注释（除非被要求）
```

## 可能的差异来源

如果你真的观察到 Qwen3-coder 和 Qwen3 之间的提示词差异，可能来自：

1. **Provider API 的差异**: 某些 provider（如 Alibaba Cloud）可能在其 API 层对 coder 版本有特殊处理
2. **自定义配置**: 你的 `opencode.json` 中可能为不同模型配置了不同的 agent 或自定义提示词
3. **自定义规则文件**: 项目中的 `AGENTS.md` 或 `CLAUDE.md` 可能包含条件性的提示词
4. **模型本身的系统提示**: 某些模型本身可能有内置的系统提示（在 OpenCode 之外）

## 如何验证实际提示词

### 方法 1: 导出会话
```bash
opencode export <session-id> > session.json
jq '.messages[] | select(.role == "system")' session.json
```

### 方法 2: 启用调试日志
```bash
export OPENCODE_LOG_LEVEL=debug
opencode
# 查看日志中的 system prompt 部分
```

### 方法 3: 查看代码
直接查看提示词文件的内容：
```bash
# Qwen 使用的提示词
cat packages/opencode/src/session/prompt/qwen.txt

# Claude 使用的提示词
cat packages/opencode/src/session/prompt/anthropic.txt
```

## 系统提示词组装顺序

完整的系统提示词由以下部分组成（按顺序）：

```
1. Header（仅 Anthropic provider）
   └─ anthropic_spoof.txt

2. Provider/Agent Prompt
   ├─ 如果有自定义 agent prompt → 使用 agent prompt
   ├─ 如果是 Codex 会话 → 跳过（使用 instructions 字段）
   └─ 否则 → SystemPrompt.provider(model) 返回的提示词

3. 环境信息
   └─ SystemPrompt.environment()
      ├─ 工作目录
      ├─ Git 仓库状态
      ├─ 平台信息
      └─ 日期

4. 自定义规则文件
   └─ SystemPrompt.custom()
      ├─ AGENTS.md（项目级和全局级）
      ├─ CLAUDE.md
      └─ 配置文件中的 instructions

5. 用户消息中的系统提示
   └─ 最后一条用户消息的 system 字段
```

## 自定义提示词方法

如果你想为 Qwen3-coder 使用不同的提示词：

### 方法 1: Agent 配置
```json
// opencode.json
{
  "agent": {
    "qwen-coder": {
      "mode": "primary",
      "model": "alibaba/qwen3-coder-32b",
      "prompt": "你是一个专门为 Qwen3-coder 优化的编程助手...\n\n..."
    }
  }
}
```

### 方法 2: 自定义规则文件
```bash
# 在项目根目录创建
echo "## Qwen Coder 特殊指令\n..." > AGENTS.md
```

### 方法 3: 修改源代码
```typescript
// packages/opencode/src/session/system.ts
export function provider(model: Provider.Model) {
  // 添加 qwen-coder 特殊处理
  if (model.api.id.toLowerCase().includes("qwen") && 
      model.api.id.toLowerCase().includes("coder")) {
    return [PROMPT_QWEN_CODER]  // 需要创建新文件
  }
  
  if (model.api.id.includes("gpt-5")) return [PROMPT_CODEX]
  // ...
}
```

## 总结

1. **OpenCode 对 Qwen3-coder 和 Qwen3 使用相同的系统提示词**（`qwen.txt`）
2. **参数设置也完全相同**（temperature=0.55, topP=1）
3. **如果你观察到差异**，可能来自：
   - Provider API 层的特殊处理
   - 你的自定义配置
   - 模型本身的内置提示
4. **可以通过 agent 配置或自定义规则文件来为特定模型定制提示词**

## 完整文档

详细信息请查看：
- `MODEL_PROMPT_RENDERING.md` - 模型提示词渲染的完整文档
- `PROMPTS_DOCUMENTATION.md` - 所有提示词文件的详细内容

如果你能提供具体的差异示例（通过会话导出），我们可以进一步分析差异的来源。
